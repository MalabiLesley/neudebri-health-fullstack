# Database Configuration
# For PostgreSQL (required if using real database instead of in-memory storage)
DATABASE_URL=postgresql://user:password@localhost:5432/neudebri_health

# Node Environment
NODE_ENV=production

# Server Port (automatically set by Vercel, but defaults to 5000 locally)
# PORT=5000

# Optional: API Base URL for frontend (if needed for absolute URLs)
# REACT_APP_API_URL=https://your-vercel-deployment.vercel.app
