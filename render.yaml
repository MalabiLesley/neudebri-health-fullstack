services:
  - type: web
    name: neudebri-health-api
    runtime: node
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    disk:
      name: data
      mountPath: /var/data
      sizeGB: 1
